# Instaladores legacy

Esta carpeta queda reservada para referencia futura. La versión MVP no ejecuta los `provision.sh` existentes.

El instalador actual replica el flujo desde Python usando perfiles YAML, validaciones, plantillas y `--dry-run`.
